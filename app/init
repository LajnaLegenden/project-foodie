#!/bin/bash
dotnet tool install --global dotnet-ef
export PATH="$PATH:/root/.dotnet/tools"
ping host.docker.internal
dotnet ef database update --no-build
dotnet "project-foodie.dll" --urls "http://0.0.0.0:5000"